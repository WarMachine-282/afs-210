# initializing objects

int_val = hash(4)
# integer value^ 
flt_val = hash(24.56)
# float value ^ 
str_val = hash("PythonIsBest")
# string value^

# printing the object values defined up top
print(f"For integer value, Hash : {int_val}")
print(f"For float value, Hash: {flt_val}")
print(f"For string value and {'PythonIsBest'}, Hash: {str_val}")
